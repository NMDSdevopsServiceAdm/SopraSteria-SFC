import { OpenEndedAnswerPipe } from './open-ended-answer.pipe';

describe('OpenEndedAnswerPipe', () => {
  it('create an instance', () => {
    const pipe = new OpenEndedAnswerPipe();
    expect(pipe).toBeTruthy();
  });
});
