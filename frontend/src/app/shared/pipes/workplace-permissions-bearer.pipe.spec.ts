import { WorkplacePermissionsBearerPipe } from './workplace-permissions-bearer.pipe';

describe('WorkplacePermissionsBearerPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkplacePermissionsBearerPipe();
    expect(pipe).toBeTruthy();
  });
});
