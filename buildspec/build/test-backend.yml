version: 0.2

env:
  variables:
      MOCHA_FILE: ./junit/test-results.xml
      NODE_ENV: example

phases:
  install:
    runtime-versions:
      nodejs: 18
  pre-build:
    commands:
      - cd lambdas/bulkUpload && npm ci
  build:
    commands:
      - cd backend && npm ci
      - npm run server:test:unit