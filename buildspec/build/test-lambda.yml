version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Downgrading npm to match node version..."
      - npm install -g npm@10
  pre_build:
    commands:
      - cd backend && npm ci
      - cd ../lambdas/bulkUpload && npm ci
  build:
    commands:
      - npm run test