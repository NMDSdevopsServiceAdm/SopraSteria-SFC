import { DataViewPermissionsPipe } from './data-view-permissions.pipe';

describe('DataViewPermissionsPipe', () => {
  it('create an instance', () => {
    const pipe = new DataViewPermissionsPipe();
    expect(pipe).toBeTruthy();
  });
});
